# 实施验证与已知问题

## 一、实施方法

采用 **自审三轮 → Subagent-Driven 开发 → 端到端验证** 的流程,共 21 个任务、35+ 次提交。

### 自审三轮(实施前)

3 个并行探查 agent 核实计划对代码库的全部假设,产出修订清单写入计划文档,**纠正了计划的多处错误**(否则实施期会编译失败或返工):

| 修订 | 计划原文 | 实际/修正 |
|------|----------|-----------|
| 数据目录字段 | `cfg.DataDir` | 顶层无此字段,实为 **`cfg.Pricing.DataDir`** |
| 错误构造器 | `NewNotFoundError`/`NewValidationError` | 不存在 → `infraerrors.NotFound` / `errors.New` |
| 前端 API 解包 | `return apiClient.get(...)` | 拦截器已解包,须 `const {data}=await...; return data` |
| 事件 DTO 字段 | 大写 `ev.ID` | 仓库惯例 snake_case `ev.id` |
| toast/Icon/Dialog | `useToast`/数字 size/`@close` | `useAppStore`/字符串 size/`@confirm`+`@cancel` |
| sub2api 真实 API | `token`/`two_fa_token` | **`access_token`/`requires_2fa`**(决定适配器能否对接) |
| 诊断截图 | `<a href>` 直链 | admin API 需鉴权 → blob 请求 |

### Subagent-Driven 开发

每个任务走 **实现者 → 规格合规审 → 代码质量审 → 修复** 闭环;数据层/锁事务/编排/收口等关键任务用更强模型深审。**评审用真实 Postgres 验证了抢占锁原子性与乐观锁时间精度**,推演了 12+ 状态机场景与 notifyIDs 索引对齐。

### 评审抓出并修复的真实缺陷

| 级别 | 缺陷 | 修复 |
|------|------|------|
| 🔴 Critical | i18n `upstream` 块错放进 `payment` 而非 `admin` → 整个前端 UI 显示裸 key | 整块位移 + 加守卫测试(vitest 加载 locale 断言) |
| 🔴 Critical | URL 归一化对 IPv6 host 破坏格式 → 二次 Parse nil → panic | `net.JoinHostPort` + nil 守卫 |
| Important | 路径 `..` 段未清理 → 关联帐号误匹配 | `path.Clean` |
| Important | webhook SSRF 自写判定弱于仓库 `isPrivateIP`(漏 CGNAT/0.0.0.0-8/nil) | 复用 `isPrivateIP` + 逐 IP 校验 |
| Important | `ResetTokens` 漏剥 `cf_expires_at` | 补齐 5 键 |
| Important | repo 事务缺 `defer tx.Rollback()`(panic 不安全) | 对齐仓库惯例 |
| Important | BrowserSolver 读 localStorage `token`(应 `auth_token`) | 修正 key |
| Important | dispatcher `cancel()` 未 defer(panic 泄漏 context) | 内联闭包 + defer |
| Important | **刷新失败返回 500** internal error(应 400) | 见 §三 |
| — | UTF-8 字节截断、email `[]string` 分支不过滤、proxy 未转义等 | 逐一修复 |

## 二、端到端验证结果

本地真实环境(后端 8090 + 前端 3000 + Postgres/Redis docker)逐项验证:

### 后端 API(全链路通过)
- 登录 → list `[]`(确认 migration 149 建表)→ **测试连接 sub2api 适配器对接本系统**:balance 9999999.52 / groups 8 / partial false(profile + groups/available + model-plaza 倒排全通)
- 创建(**响应凭证脱敏**,仅 tail)→ 刷新(余额/分组/模型)→ **balance_low 告警事件 notified:true**(diff + 状态机 + 通知编排)
- Token 一次性明文(`sk-...`)+ 有效 API 地址 / 关联帐号匹配 / **重复刷新告警不重复**(跨越式去重)
- **webhook SSRF 开关精确生效**:默认私网→400 SSRF 拒绝;开 allow_private→放行后变连接失败(非 SSRF)

### 前端 UI(playwright,全流程通过)
- 「上游管理」菜单就位、标题/描述 i18n 全中文(裸 key 已修复)
- 列表表格(余额/状态徽标/分组8/模型16,**R2.1 瘦身快照保留分组→模型数正确**)
- 详情抽屉 6 Tab + tablist 可访问性(role/aria)
- 变更历史「余额不足」+「已通知」(**snake_case 数据流正确**)
- 暗色模式对比度良好、0 console errors

### 质量状态
- 后端:`go build` + `go vet` 干净,**36 包测试全绿 0 失败**;`make generate` 无 diff(wire 全图闭合、ent 一致)
- 前端:build 成功;i18n 守卫测试 + 校验 TDD(8)通过
- fork 守则:新 wire provider 全在 `wire_fork.go`,未碰上游 `wire.go`

## 三、已知问题

### 1. 刷新失败曾返回 500(已修复 → 400)

`response.ErrorFrom` 对非 `ApplicationError` 的普通 error 默认映射 500。`RefreshProvider` 过盾/采集失败返回普通 error,导致 handler 返回 500 internal error。
**修复**(commit `c52bbf48`):刷新失败(非 RefreshInProgress)→ `BadRequest`(400)+ 友好信息;`NotFound` 仍走 404。前端从详情可见持久化的 `last_error`。

### 2. CloakBrowser 在 Docker Desktop for Mac 下 TLS 握手失败(外部依赖/环境)

CloakBrowser 的 patched Chromium 打开 HTTPS 时 `handshake failed; net_error -100`(arm64/amd64 均复现),而容器内系统 `curl` 同站点 200。判断为 **Docker-Mac NAT + MTU 导致 Chromium TLS 大包被丢弃**的经典问题,非本模块代码问题。
本模块集成链路(chromedp 连 CloakBrowser、传 fingerprint-seed、导航、cf_clearance 轮询、超时优雅返回 400)**已验证正常**。规避见 [本地测试与部署.md §三](本地测试与部署.md)(低 MTU 网络);Linux 生产通常无此问题。

### 3. `read-only /app` 日志降级(无害)

`make dev-backend` 在宿主机跑时,默认日志文件路径 `/app/data/logs` 只读,降级到 stdout(`LOG_OUTPUT_TO_STDOUT=true` 兜底)。仅 WARN,不影响功能。如需消除见故障排查表。

### 4. 前端 16 个 pre-existing 测试失败(非本模块引入)

`pnpm test:run` 有 16 个失败,均为既有回归(`AccountUsageCell`/`*DistributionChart`/`EmailVerifyView`/`UsageView`/`UsageTable`,chart canvas mock / image billing 断言等),本模块未触碰这些文件;`AppSidebar`/`upstreamValidators`/i18n 守卫测试均通过。

## 四、后续优化建议(非阻塞)

- `UpstreamMonitorRunner` 优雅停机:当前不注册 shutdown cleanup(R1.7 决策,靠 5min stale 锁兜底),如需进程退出时优雅 drain in-flight 刷新,可将刷新 goroutine 纳入 runner 的 WaitGroup。
- BrowserSolver 截图正则耦合中文「诊断截图:」前缀(前端解析失败截图文件名用),若错误消息多语言化需同步。
- sub2api 适配器 `profile.Code != 0` 但 HTTP 200 时静默 partial(本仓库 profile 认证失败走 HTTP 401,实际不触发);可加显式上抛增强健壮性。
